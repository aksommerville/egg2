/* graf.h
 * Client-side rendering helper for Egg.
 */
 
#ifndef GRAF_H
#define GRAF_H

struct graf {
  int TODO;
};

static inline void graf_reset(struct graf *graf) {}
static inline void graf_flush(struct graf *graf) {}

#endif
