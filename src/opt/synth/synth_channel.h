/* synth_channel.h
 * Tuned signal generator, multiple voices.
 */
 
#ifndef SYNTH_CHANNEL_H
#define SYNTH_CHANNEL_H

struct synth_channel {
  int TODO;
};

static inline void synth_channel_del(struct synth_channel *channel) {}

#endif
